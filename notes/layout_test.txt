basic largest bsp
- sizes -
let, a = full,
let, b = half,
let, c = quarter,
let, d = eighths,
let, e = sixteenths,
- operations -
let, + = open a window,
let, - = close a window,
- data -
let, n, as iteration, where n is any integer,
let, [s] as size of the newly spawned window,
let, {s(x)}, where s is size, and x is the grid-like position in (row,column) of a window

- - , ---
1 +, [a]{a(1,1)}
2 +, [b]{b(1,1),b(1,2)}
3 +, [c]{c(1,1),c(1,2),b(1,3)}
4 +, [c]{c(1,1),c(1,2),c(1,3),c(1,4)}
5 +, [d]{c(1,1),d(1,2),d(2,2),c(1,3),c(1,4)}
6 +, [d]{c(1,1),d(1,2),d(2,2),c(1,3),d(1,4),d(2,4)}
7 +, [d]{d(1,1),d(2,1),d(1,2),d(2,2),c(1,3),d(1,4),d(2,4)}
8 +, [d]{d(1,1),d(2,1),d(1,2),d(2,2),d(1,3),d(2,3),d(1,4),d(2,4)}
9 +, [e] ah i hit a limit to this coordinate system, basically: row,col d(2,2) is further split into 2 columns
10 -, same as 8
11 -, same as 7
- - , ---

spiral bsp
- sizes -
let, a = full,
let, b = half,
let, c = quarter,
let, d = eighths,
let, e = sixteenths,
- operations -
let, + = open a window,
let, - = close a window,
- data -
let, n, as iteration, where n is any integer,
let, [s], s are the window's sizes, sorted from first to last opened window,
let, ascii as layout representation

- - , ---
1, +, [a]
+-----+-----+-----+-----+
|                       |
|           1           |
|                       |
+-----+-----+-----+-----+
2, +, [b,b]
+-----+-----+-----+-----+
|           |           |
|     1     |     2     |
|           |           |
+-----+-----+-----+-----+
3, +, [b,?,?]
+-----+-----+
|           |
|     1     +
|           |
+-----+-----+
4, +, [b,?,?,?]
+-----+-----+
|           |
|     1     +
|           |
+-----+-----+
5, +, [b,c,d,e,e]
+-----+-----+-----+-----+
|           |     2     |
|     1     +-----+-----+
|           |  3  +-----+ 4
+-----+-----+-----+-----+ 5
- - , ---

[20:48:16] Layout update: bsp, Resize WMLayout -> (5, 1)
----------------------------------------------------------------------
[20:48:18] Key 'alt+n' -> Run 'Notepad'
    - Creating Notepad Window 1:
      - Mount & Show all components.
      - Move -> (0, 0), Resize Window -> (86, 37)
      - Focus -> TextArea(id='app-content')

----------------------------------------------------------------------
[20:48:21] Key 'alt+n' -> Run 'Notepad'
    - Creating Notepad Window 2:
      - Mount & Show all components.
      - Blur previous window's TextArea.
      - Move -> (0, 0), Resize Window -> (43, 37)
      - Focus -> new TextArea(id='app-content')

----------------------------------------------------------------------
[20:48:23] Key 'alt+n' -> Run 'Notepad'
    - Creating Notepad Window 3:
      - Mount & Show all components.
      - Blur previous window's TextArea.
      - Move -> (0, 0), Resize Window -> (0, 0)
      - Focus -> new TextArea(id='app-content')

----------------------------------------------------------------------
[20:48:26] Key 'alt+n' -> Run 'Notepad'
    - Creating Notepad Window 4:
      - Mount & Show all components.
      - Blur previous window's TextArea.
      - Move -> (0, 0), Resize Window -> (0, 0)
      - Focus -> new TextArea(id='app-content')

ultra-wide
1, [a]
+-----+-----+-----+-----+
|                       |
|           1           |
|                       |
+-----+-----+-----+-----+
2, [b,b]
+-----+-----+-----+-----+
|           |           |
|     1     |     2     |
|           |           |
+-----+-----+-----+-----+
3, [c,b,c]
+-----+-----+-----+-----+
|     |           |     |
|  1  |     2     |  3  |
|     |           |     |
+-----+-----+-----+-----+
4, [c,b,d,d]
+-----+-----+-----+-----+
|     |           |  4  |
|  1  |     2     +-----+
|     |           |  5  |
+-----+-----+-----+-----+
n...horizontal stack right
+-----+-----+-----+-----+
|     |           |-----|
|  1  |     2     |-----|
|     |           |-----|
+-----+-----+-----+-----+
